@model IEnumerable<databasewithviews.Models.Customer>

@{
    ViewData["Title"] = "allcust";
}

<h2>allcust</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.CustomerId)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.CustomerName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.CustomerProjname)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.CustomerCity)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CustomerId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CustomerName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CustomerProjname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CustomerCity)
            </td>
            <td>
  <a href="/Customers/detail?CustomerId=@item.CustomerId" class="btn btn-info"> Details </a>
                <a href="/Customers/delete?CustomerId=@item.CustomerId" class="btn btn-danger"> Delete </a>
                <a href="/Customers/edit?CustomerId=@item.CustomerId" class="btn btn-warning"> Edit </a>
            </td>
        </tr>
}
    </tbody>
</table>
